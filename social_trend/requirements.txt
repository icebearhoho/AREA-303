# ============================================================
# Feature #08 - Social Trend Analyzer - Dependencies
# ============================================================

# --- Core ---
pandas>=2.0
pyarrow>=14.0            # doc/ghi .parquet
matplotlib>=3.7          # figures

# --- Sentiment Engine (cong cu doc lap) ---
scikit-learn>=1.3        # baseline TF-IDF + LogisticRegression (fallback)
transformers>=4.40       # XLM-RoBERTa fine-tuned
torch>=2.2               # backend cho transformers

# --- Dashboard ---
streamlit>=1.30
plotly>=5.18

# --- Trend Detector: real-time Google Trends ---
requests>=2.28           # goi SerpApi (on dinh, can SERPAPI_KEY trong .env)
python-dotenv>=1.0       # nap SERPAPI_KEY tu .env
pytrends>=4.9            # phuong an mien phi (khong chinh thuc, hay bi 429)
