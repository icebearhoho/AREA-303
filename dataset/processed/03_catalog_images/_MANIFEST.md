# Pipeline 3 (Catalog / Images) — Cleaned Outputs

Generated by `clean_pipeline3.py`. All parquet.

| Clean output | Rows | Cols | Feeds | Key transformations |
|---|---|---|---|---|
| `tiki_catalog_clean.parquet` | 41,576 | 18 | #02, #03, #09, #17 | combined 6 category files; dropped `Unnamed: 0` + junk `date_created`; deduped on `id` (27 dups); VND prices→numeric; added `discount_pct` |
| `asos_catalog_clean.parquet` | 30,501 | 9 | #02 | `price`→numeric (£, 4.5–550); dropped 344 fully-duplicated rows |
| `deepfashion_attributes_clean.parquet` | 52,591 | 7 | #07, #17 | attributes-only table (category1-3, color, description, text, item_ID); embedded image bytes **excluded** to keep it light |

## Notes for modelers
- **tiki (#02/#03/#09/#17):** your VND catalog; `discount_pct` derived from `original_price` vs `price`. Many items have 0% discount (normal). Use `quantity_sold`, `rating_average`, `review_count` for demand/recsys signals.
- **asos (#02):** prices are GBP. The original file repeated each product per size; deduped to unique rows. `size` column is a comma-joined string.
- **deepfashion (#07/#17):** this clean file is the **attributes/index table** (no pixels). The actual images stay in the raw parquet (`dataset/03_catalog_images/Marqo_deepfashion_inshop/...`), joinable by `item_ID`. Load images from raw only when computing CLIP/embeddings; use this table for filtering/metadata.
